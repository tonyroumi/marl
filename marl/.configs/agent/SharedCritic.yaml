agent_class: BasicMARLAgent

actor_0:
  actor_observations: [
    [robot0_proprio-state, 39] #(39,)
  ]

actor_1:
  actor_observations: [
    [robot1_proprio-state, 39] #(39,)
  ]

critic:
  critic_observations: [
    [robot0_proprio-state, 39], #(39,)
    [robot1_proprio-state, 39], #(39,)
    [object-state, 17] #(17,)
  ]

kwargs: 
  normalize_observations: true
  num_transitions_per_env: 1000
  save_interval: 1000
